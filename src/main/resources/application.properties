spring.application.name=learn-spring-boot
logging.level.org.springframework = debug

spring.profiles.active=dev
# to set the profile active.   

# These values can be overridden if the other profile selected, has the same variable defined with other values
currency-service.url = http://default.risafe.com
currency-service.username = defaultusername
currency-service.key = defaultkey





